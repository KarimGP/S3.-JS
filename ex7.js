var objectes = ['taula', 'cadira', 'ordinador', 'llibreta'];
var contador;

for( contador=0; contador < 4; contador++ ) {
    console.log("L'objecte " + objectes[contador] + " es troba a la posició " + contador + ".");
}
